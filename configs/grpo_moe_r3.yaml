cluster:
  gpus_per_node: 8
  num_nodes: 1

policy:
  model_name: Qwen/Qwen3-30B-A3B
  training_backend: megatron
  generation_backend: atom
  max_total_sequence_length: 4096
  train_global_batch_size: 64
  train_micro_batch_size: 4

  training:
    megatron_cfg:
      tensor_parallel_size: 4
      expert_parallel_size: 2
      num_experts: 128
      moe_grouped_gemm: true

  generation:
    atom_cfg:
      tensor_parallel_size: 4
      kv_cache_dtype: auto

algorithm:
  name: grpo
  grpo:
    num_generations: 8
    kl_coeff: 0.0
    clip_ratio: 0.2

quantization:
  rollout:
    precision: bf16
  training:
    fp8: null
  rollout_correction:
    enabled: false

moe:
  r3:
    enabled: true
    record_router_logits: true
    replay_mode: distribution

reward:
  type: function
  function: math_reward
  dataset: nvidia/OpenMathInstruct-2

checkpointing:
  checkpoint_dir: results/grpo_moe_r3
  save_steps: 50

logger:
  wandb_enabled: true
  wandb:
    project: lumenrl
    name: grpo-qwen3moe-r3

num_training_steps: 500
seed: 42
